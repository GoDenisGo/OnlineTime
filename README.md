# OnlineTime
 Online clock that tells the time.

## Running the project:

Run `npm install` to download the node modules.
Run `npm start .` to boot the server.
Open your browser and enter `127.0.0.1:8080` into the address bar.

A website should load with three clocks and a message.